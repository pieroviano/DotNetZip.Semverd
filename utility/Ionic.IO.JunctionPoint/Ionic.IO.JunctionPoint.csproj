<Project Sdk="Microsoft.NET.Sdk">

	<PropertyGroup>
		<!-- Win32 reparse-point P/Invoke: the assembly is portable, the behaviour is Windows-only. -->
		<TargetFramework>netstandard2.0</TargetFramework>
		<LangVersion>latest</LangVersion>
		<AssemblyName>Ionic.IO.JunctionPoint</AssemblyName>
		<RootNamespace>Ionic.IO</RootNamespace>

		<!-- assembly metadata, formerly Properties\AssemblyInfo.cs -->
		<Version>1.0.0.0</Version>
	</PropertyGroup>

</Project>